# Servidor MCP local (transporte stdio). Ya no hay servidor HTTP: al pasar de la nube
# al named pipe local se eliminaron fastapi, uvicorn, gunicorn, starlette,
# sse-starlette, Authlib, python-multipart, redis/fakeredis y python-dotenv.
#
# Versiones verificadas en Python 3.12.10 sobre la cadena completa
# (cliente MCP -> stdio -> main.py -> named pipe -> add-in .NET 8).

# Dependencias directas
fastmcp==2.14.5
anyio==4.12.1

# Transitivas, fijadas para que el empaquetado con PyInstaller sea reproducible
mcp==1.29.0
pydantic==2.13.4
